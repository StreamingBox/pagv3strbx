{
  "name": "strbx-netflix-tv-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Worker aislado para automatizar el registro de TVs en Netflix",
  "engines": {
    "node": ">=20 <21",
    "npm": ">=10"
  },
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "playwright": "^1.63.0"
  }
}
